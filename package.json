{
  "name": "nova-play-webos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "preview": "vite preview",
    "package:webos": "npm run build && ares-package webos-app -o packages"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "hls.js": "^1.6.16"
  }
}
